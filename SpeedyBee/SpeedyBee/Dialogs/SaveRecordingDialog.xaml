<Window x:Class="SpeedyBee.Dialogs.SaveRecordingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Recording" 
        Height="250" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Save Recording As:" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" 
                   Text="Recording Name:" 
                   Margin="0,0,0,5"/>
        
        <TextBox Grid.Row="2" 
                 x:Name="txtRecordingName"
                 Margin="0,0,0,20"
                 Padding="5"/>

        <GroupBox Grid.Row="3" 
                  Header="Save Location" 
                  Margin="0,0,0,20">
            <StackPanel Margin="10">
                <RadioButton x:Name="rbCsv" 
                            Content="Save as CSV file" 
                            IsChecked="True" 
                            Margin="0,0,0,10"/>
                <RadioButton x:Name="rbPostgres" 
                            Content="Save to PostgreSQL database"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Save" 
                    Width="80" 
                    Height="30" 
                    Margin="0,0,10,0"
                    Click="BtnSave_Click"
                    IsDefault="True"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Height="30"
                    Click="BtnCancel_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
